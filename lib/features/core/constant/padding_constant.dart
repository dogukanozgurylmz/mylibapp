import 'package:flutter/material.dart';

class PaddingConstant {
  static const appPaddingHor = EdgeInsets.symmetric(horizontal: 10);
  static const paddingAll10 = EdgeInsets.all(10);
  static const homeBookcasePadding = EdgeInsets.all(10);
  static const formPadding = EdgeInsets.all(16);
}
